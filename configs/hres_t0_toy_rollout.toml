# Rollout campaign. splice_path is the local HRES-T0 zarr this run must fit.
#
# Same timestamp construction as splice.toml (overlapping windows, union).
# Coverage: this run's needed_times must be a SUBSET of the splice time
# index. Extra times on disk are OK; a missing t-6h or lead raises.
#
# Matches splice.toml: 27 packed inits × 40 steps, hole-clean from 2022-01-12.

splice_path = "data/splice_hres_t0_toy_jan_clean.zarr"

first_init = "2022-01-12T12:00:00"
n_inits = 27
init_stride_hours = 12
n_rollout_steps = 40
