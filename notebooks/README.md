# Notebooks

- `notebooks/` — committed exploration notebooks (outputs stripped by nbstripout)
- `notebooks/_scratch/` — local-only scratch pads (gitignored)

Workflow: prototype here → promote stable code into modules (e.g. `src/` once the package layout is chosen) → run experiments via `scripts/` + `configs/`.
